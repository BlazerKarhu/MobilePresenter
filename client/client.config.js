/**
 * Address and port of the server the application will attempt to connect to.
 * IMPORTANT: local ip address will only function during testing, a specific ip should be defined for production use.
*/
export const baseUrl = `http://localhost:3000/`
