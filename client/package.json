{
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "start": "expo start || npm install && expo start",
    "android": "expo start --android || npm install && expo start --android",
    "ios": "expo start --ios || npm install && expo start --ios",
    "web": "expo start --web || npm install && expo start --web",
    "eject": "expo eject || npm install && expo eject"
  },
  "dependencies": {
    "expo": "~44.0.0",
    "expo-status-bar": "~1.2.0",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "react-native": "0.64.3",
    "react-native-web": "0.17.1"
  },
  "devDependencies": {
    "@babel/core": "^7.12.9"
  },
  "private": true
}
