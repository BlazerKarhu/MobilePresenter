{
  "name": "presenterserver",
  "version": "1.0.0",
  "description": "Serves posts via endpoints.",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js || npm install && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
